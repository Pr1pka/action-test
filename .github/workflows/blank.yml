name: first-action

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  start:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v1
      - name: Print my initials
        run: ./script.sh
